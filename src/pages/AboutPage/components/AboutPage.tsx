import { PageMainContent } from '@/widgets/PageMainContent';
import { memo } from 'react';

export const AboutPage = memo(() => {
	return <PageMainContent data-testid='AboutPage'>О сайте</PageMainContent>;
});
